apiVersion: v1
kind: Service
metadata:
  name: node-hello-world-service
  labels:
    app: node-hello-world
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: node-hello-world
